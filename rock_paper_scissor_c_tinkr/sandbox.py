def add_numbers(a: int, b: int) -> int:
    """Return the sum of two numbers.

    Args:
        a: First number.
        b: Second number.

    Returns:
        The sum of a and b.
    """
    return a + b

result = add_numbers(5, 3)
print(result)